// This component is deprecated since we removed local storage and syncing
// Keeping it to prevent import errors, but it doesn't render anything
export default function SyncStatusIndicator() {
    return null;
}